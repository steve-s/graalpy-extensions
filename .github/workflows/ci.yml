name: Build Artifacts

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 0 * * 0'

env:
  graalvm_version: '25i3-ea'

jobs:
  # This is done as one single separate step to work around rate limits for the API
  maven_bundle_url:
    runs-on: ubuntu-latest
    outputs:
      maven_bundle_url: ${{ steps.set_url.outputs.maven_bundle_url }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set Maven Bundle URL
        id: set_url
        run: |
          tmp_file="$(mktemp)"
          ./scripts/maven-bundle-url.sh | tee "$tmp_file"
          echo "maven_bundle_url=$(tail -n 1 "$tmp_file")" >> $GITHUB_OUTPUT
        shell: bash

  pre_commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install pre-commit
        run: python -m pip install --upgrade pip pre-commit
        shell: bash

      - name: run pre-commit
        run: pre-commit run --all-files --show-diff-on-failure
        shell: bash

  build:
    runs-on: ${{ matrix.os }}
    # pre_commit not needed, but is in 'needs' to avoid wasting CI cycles in case it doesn't pass
    needs: [maven_bundle_url, pre_commit]
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          distribution: 'graalvm'
          java-version: ${{ env.graalvm_version }}

      - name: Setup Polyglot Maven bundle
        run: ./scripts/maven-bundle-setup.sh "${{ needs.maven_bundle_url.outputs.maven_bundle_url }}"
        shell: bash

      - name: Build project (skip tests)
        run: mvn --batch-mode -DskipTests package verify
        shell: bash

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts-${{ runner.os }}
          path: ./**/target/**

  integration:
    needs: [build, maven_bundle_url]
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        test_name: [ test_jbang_integration.py, test_maven_plugin.py, test_gradle_plugin.py ]
        include:
          # We need to skip native-image tests on Gradle and MacOS runners: it seems that with
          # Gradle the tests require more RAM and cannot reliably build native image.
          # For other tests on MacOS we run just native-image smoke tests, because while building
          # native-image seems to be feasible there, it is quite slow
          - test_name: test_gradle_plugin.py
            os: macos-latest
            extra_test_args: "--native-image=none"
          - test_name: test_maven_plugin.py
            os: macos-latest
            extra_test_args: "--native-image=smoke"
          - test_name: test_jbang_integration.py
            os: macos-latest
            extra_test_args: "--native-image=smoke"
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set integration tests flags
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "TEST_FLAGS=--skip-long-running" >> $GITHUB_ENV
          else
            echo "TEST_FLAGS=" >> $GITHUB_ENV
          fi
        shell: bash

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts-${{ runner.os }}
          path: .

      - name: Set up GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          distribution: 'graalvm'
          java-version: latest-ea

      - name: Setup Polyglot Maven bundle
        run: ./scripts/maven-bundle-setup.sh "${{ needs.maven_bundle_url.outputs.maven_bundle_url }}"
        shell: bash

      - name: Run Maven install
        run: ./mvnw --batch-mode -DskipTests install
        shell: bash

      - name: Install JBang (Linux/macOS)
        if: runner.os != 'Windows' && matrix.test_name == 'test_jbang_integration.py'
        run: |
          curl -Ls https://sh.jbang.dev | bash
          echo "$HOME/.jbang/bin" >> $GITHUB_PATH
        shell: bash

      - name: Install JBang (Windows)
        if: runner.os == 'Windows' && matrix.test_name == 'test_jbang_integration.py'
        run: choco install jbang
        shell: pwsh

      # - name: Setup MSVC on Windows
      #   if: runner.os == 'Windows'
      #   uses: ilammy/msvc-dev-cmd@v1

      - name: Add git utils to $PATH on Windows
        if: runner.os == 'Windows'
        run: echo "C:\Program Files\Git\usr\bin" >> $env:GITHUB_PATH

      # We scrape the output of integration tests for paths to interesting log files,
      # so that we can then upload them in the following actions/upload-artifact
      - name: Run integration tests
        id: tests_run
        run: |
          ./mvnw --batch-mode -N exec:java@integration-tests -Dintegration.tests.args="${{ matrix.test_name }} ${{ matrix.extra_test_args }} $TEST_FLAGS"
        shell: bash

      # /var/folders/sw/**/T/jbang*native-image
      # ^ not using this path on MacOS, because it causes
      - name: Upload JBang native-image log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: jbang-native-image-log
          path: |
            /tmp/jbang*native-image

  unit-tests:
    needs: [build, maven_bundle_url]
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        tests: [unit-cp, unit-mp, unit-it-mp, isolate-it]
        exclude: # TODO: isolate resources seem to not be in the bundle for those
          - os: macos-latest
            tests: isolate-it
          - os: windows-latest
            tests: isolate-it
        include:
          - tests: unit-cp
            maven_args: "test"
          - tests: unit-mp
            maven_args: >-
              -pl org.graalvm.python.embedding -Ptest.modules -am test
          - tests: unit-it-mp
            maven_args: >-
              -pl org.graalvm.python.embedding -Ptest.modules.integration -Dtest=org.graalvm.python.embedding.test.integration.** -am test
          - tests: isolate-it
            maven_args: >-
              -pl org.graalvm.python.embedding -Pisolate -Dpolyglot.engine.AllowExperimentalOptions=true -Dpolyglot.engine.SpawnIsolate=true -Dpolyglot.engine.IsolateMode=external -Dtest=org.graalvm.python.embedding.test.integration.** -am test
    name: "${{ matrix.tests }} (${{ matrix.os }})"
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts-${{ runner.os }}
          path: .

      - name: Set up GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          distribution: 'graalvm'
          java-version: ${{ matrix.tests == 'isolate-it' && 'latest-ea' || env.graalvm_version }}

      - name: Setup Polyglot Maven bundle
        run: ./scripts/maven-bundle-setup.sh "${{ needs.maven_bundle_url.outputs.maven_bundle_url }}"
        shell: bash

      - name: Run Maven install
        run: mvn --batch-mode -DskipTests install
        shell: bash

      - name: Run Maven tests
        run: mvn --batch-mode ${{ matrix.maven_args }}
        shell: bash

  pyinterfacegen:
    # Run pyinterfacegen's Gradle-based tests on all OSes to ensure cross-platform health.
    # Keep independent of Maven bundle setup, since it uses Gradle composite builds.
    needs: [pre_commit]
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    name: "pyinterfacegen Gradle tests (${{ matrix.os }})"
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          distribution: 'graalvm'
          java-version: ${{ env.graalvm_version }}

      - name: Run Gradle tests (Linux/macOS)
        if: runner.os != 'Windows'
        working-directory: pyinterfacegen
        run: ./gradlew --no-daemon --info check
        shell: bash

      - name: Run Gradle tests (Windows)
        if: runner.os == 'Windows'
        working-directory: pyinterfacegen
        run: .\\gradlew.bat --no-daemon --info check
        shell: pwsh

  build_graalpy_sandboxed_mcp:
    runs-on: ${{ matrix.os }}
    needs: [pre_commit, maven_bundle_url]
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: graalpy-sandboxed-mcp-linux-amd64
            artifact_suffix: ""
          # Other platforms cannot be built against the bundle because the bundle only contains the isolate for linux-amd64 (it does contain artifacts for other platforms, but they are incomplete)
          # - os: ubuntu-24.04-arm
          #   artifact_name: graalpy-sandboxed-mcp-linux-aarch64
          #   artifact_suffix: ""
          # OOMs when building graalpy
          # - os: macos-latest
          #   artifact_name: graalpy-sandboxed-mcp-macosx-aarch64
          #   artifact_suffix: ""
          # - os: windows-latest
          #   artifact_name: graalpy-sandboxed-mcp-windows-amd64
          #   artifact_suffix: .exe
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          distribution: 'graalvm'
          java-version: latest-ea

      - name: Setup Polyglot Maven bundle
        run: ./scripts/maven-bundle-setup.sh "${{ needs.maven_bundle_url.outputs.maven_bundle_url }}"
        shell: bash

      - name: Build graalpy-sandboxed-mcp native image and run integration tests
        run: >-
          mvn --batch-mode
          -pl graalpy-sandboxed-mcp -am
          -Dpackaging=native-image
          verify
        shell: bash

      - name: Add the license file
        run: cp common/GraalVM_GFTC_License.txt graalpy-sandboxed-mcp/target/
        shell: bash

      - name: Upload graalpy-sandboxed-mcp binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: |
            graalpy-sandboxed-mcp/target/graalpy-sandboxed-mcp${{ matrix.artifact_suffix }}
            graalpy-sandboxed-mcp/target/GraalVM_GFTC_License.txt
          if-no-files-found: error

  build_graalpy_sandboxed_cli:
    runs-on: ${{ matrix.os }}
    needs: [pre_commit, maven_bundle_url]
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: graalpy-sandboxed-linux-amd64
            artifact_suffix: ""
          # Other platforms cannot be built against the bundle because the bundle only contains the isolate for linux-amd64 (it does contain artifacts for other platforms, but they are incomplete)
          # - os: ubuntu-24.04-arm
          #   artifact_name: graalpy-sandboxed-linux-aarch64
          #   artifact_suffix: ""
          # OOMs when building graalpy
          # - os: macos-latest
          #   artifact_name: graalpy-sandboxed-macosx-aarch64
          #   artifact_suffix: ""
          # - os: windows-latest
          #   artifact_name: graalpy-sandboxed-windows-amd64
          #   artifact_suffix: .exe
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          distribution: 'graalvm'
          java-version: latest-ea

      - name: Setup Polyglot Maven bundle
        run: ./scripts/maven-bundle-setup.sh "${{ needs.maven_bundle_url.outputs.maven_bundle_url }}"
        shell: bash

      - name: Build graalpy-sandboxed-cli native image and run integration tests
        run: >-
          mvn --batch-mode
          -pl graalpy-sandboxed-cli -am
          -Dpackaging=native-image
          verify
        shell: bash

      - name: Add the license file
        run: cp common/GraalVM_GFTC_License.txt graalpy-sandboxed-cli/target/
        shell: bash

      - name: Upload graalpy-sandboxed-cli binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: |
            graalpy-sandboxed-cli/target/graalpy-sandboxed${{ matrix.artifact_suffix }}
            graalpy-sandboxed-cli/target/GraalVM_GFTC_License.txt
          if-no-files-found: error
